{
  "name": "Arch Linux",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "root",
  "mounts": [
            "source=${localEnv:HOME}/.ssh,target=/root/.ssh,type=bind,consistency=cached",
        ],
  "customizations": {
    "vscode": {
      "extensions": []
    }
  }
}
